import { SettingsPanel } from '@/components/settings/SettingsPanel';

export const metadata = {
  title: 'Shop Settings & RBAC — Akshit ERP',
};

export default function SettingsPage() {
  return <SettingsPanel />;
}
