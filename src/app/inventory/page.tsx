import { InventoryManager } from '@/components/inventory/InventoryManager';

export const metadata = {
  title: 'Inventory & Stock Audit — Akshit ERP',
};

export default function InventoryPage() {
  return <InventoryManager />;
}
