import { BarcodePrinterStudio } from '@/components/labels/BarcodePrinterStudio';

export const metadata = {
  title: 'Barcode Label Printing Studio — Akshit ERP',
};

export default function LabelsPage() {
  return <BarcodePrinterStudio />;
}
