import { ProductCatalog } from '@/components/products/ProductCatalog';

export const metadata = {
  title: 'Product Master Catalog — Akshit ERP',
};

export default function ProductsPage() {
  return <ProductCatalog />;
}
